version: "3.9"

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: canboard
      POSTGRES_USER: canboard
      POSTGRES_PASSWORD: canboard
    ports:
      - "5432:5432"